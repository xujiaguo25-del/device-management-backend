spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/device_management_db
    username: postgres
    password: 123456

  sql:
    init:
      mode: always

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
