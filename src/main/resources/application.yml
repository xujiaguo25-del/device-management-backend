server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: device-management-system
  profiles:
    active: dev

logging:
  level:
    root: INFO
    com.device: DEBUG

jwt:
  secret: 124124
  expiration: 86400000

